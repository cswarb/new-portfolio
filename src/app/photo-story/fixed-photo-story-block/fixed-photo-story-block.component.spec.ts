import { FixedPhotoStoryBlockComponent } from './fixed-photo-story-block.component';

describe('FixedPostBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedPhotoStoryBlockComponent();
    expect(directive).toBeTruthy();
  });
});

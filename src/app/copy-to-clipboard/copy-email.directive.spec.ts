import { CopyEmailDirective } from './copy-email.directive';

describe('CopyEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyEmailDirective();
    expect(directive).toBeTruthy();
  });
});

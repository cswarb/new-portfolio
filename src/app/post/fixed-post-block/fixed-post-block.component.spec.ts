import { FixedPostBlockDirective } from './fixed-post-block.directive';

describe('FixedPostBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedPostBlockDirective();
    expect(directive).toBeTruthy();
  });
});
